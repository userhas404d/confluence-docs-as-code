# confluence-docs-as-code.001-confluence-mkdocs-pull Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-02-27

## Active Technologies

- Node.js >= 20 (ES modules, `"type": "module"`) + axios (HTTP client), axios-retry (retry policy), yaml (YAML generation), form-data — all already present in `package.json` (001-confluence-mkdocs-pull)

## Project Structure

```text
src/
tests/
```

## Commands

# Add commands for Node.js >= 20 (ES modules, `"type": "module"`)

## Code Style

Node.js >= 20 (ES modules, `"type": "module"`): Follow standard conventions

## Recent Changes

- 001-confluence-mkdocs-pull: Added Node.js >= 20 (ES modules, `"type": "module"`) + axios (HTTP client), axios-retry (retry policy), yaml (YAML generation), form-data — all already present in `package.json`

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
